# IO

這個函式庫是在原生的輸出運算子 `<<<` 之上構建的，提供比較方便的輸出操作。

## 匯入
這個套件在官方提供的編譯器中都叫 `io`，如果是由第三方所提供的編譯器，請參考他們的說明文件。
```
>>> "io"
```

## 函式

- `print(fmt, ...args)`：輸出 `fmt` 字串，並且會把字串中的 `%s` 用 `args` 的內容逐一取代。
- `println(fmt, ...args)`：和 `print` 一樣，只是會在結尾輸出一個換行。
